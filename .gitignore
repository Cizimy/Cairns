# --- General ---
# OS generated files
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Editor/IDE specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.komodoproject
*~
*.swp
*.bak
*.tmp

# Log files
*.log
logs/
npm-debug.log*
yarn-error.log*

# --- Language & Framework specific ---
# Node.js (for linters, validators, SSG, custom scripts etc.)
node_modules/
.npm/
.pnp.*
*.tsbuildinfo
# package-lock.json / yarn.lock: Generally recommended to commit, but discuss with the team.
# If not committing lock files, uncomment the lines below:
# package-lock.json
# yarn.lock

# Python (for custom validation scripts etc.)
__pycache__/
*.py[cod]
*$py.class
.Python
build/
develop-eggs/
# dist/ # Potentially conflicts with SSG build output, consider specific path if needed
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# Environment directories
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# --- Build artifacts & Tools ---
# Static Site Generator (SSG) build output (Adjust based on chosen tool in Task 1.2.9)
# Common directories:
# build/ # Already listed under Python, potentially keep generic
dist/
public/
out/
# Specific tool examples:
.docusaurus/
build-docusaurus/ # Docusaurus
site/ # MkDocs
.astro/ # Astro

# RAG Index file (Generated by CI as planned in Task 2.5)
index.ndjson

# Test coverage reports
.coverage
coverage/
htmlcov/
nosetests.xml
coverage.xml

# --- Sensitive data ---
# Environment variables / secrets
.env*
!.env.example # Keep example files if used
*.pem
*.key
credentials.*
config.local.*
secrets.*

# --- Miscellaneous ---
# Microsoft Office temporary files
~$*

# Add project specific temporary files or build outputs below if necessary
# Ignore development documents
_dev_docs/