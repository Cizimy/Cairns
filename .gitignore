# === General ===
# OS generated files
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Editor/IDE specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.komodoproject
*~
*.swp
*.bak
*.tmp

# Log files
*.log
logs/ # Ensure trailing slash for directories
npm-debug.log*
yarn-error.log*
yarn-debug.log*

# === Language & Framework specific ===

# --- Node.js (for linters, validators, SSG, custom scripts etc.) ---
node_modules/
.npm/
.pnp.*
*.tsbuildinfo

# Lockfiles (package-lock.json / yarn.lock):
# It is STRONGLY recommended to commit lock files to ensure reproducible builds
# across different environments (developers, CI/CD).
# Discuss with the team only if there's a very specific, documented reason not to.
# If NOT committing lock files (which is discouraged), uncomment the lines below:
# package-lock.json
# yarn.lock

# --- Python (for custom validation scripts etc.) ---
__pycache__/
*.py[cod]
*$py.class
.Python
# Build artifacts (assuming no standard package building for this project)
# If package building (sdist, wheel) becomes necessary, uncomment relevant lines below
# build/
# develop-eggs/
# dist/
# downloads/
# eggs/
# .eggs/
# lib/
# lib64/
# parts/
# sdist/
# var/
# wheels/
# *.egg-info/
# .installed.cfg
# *.egg
# MANIFEST

# Environment directories (VERY important to ignore)
.env
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# --- VBA (for vba-stack, if developed within repo) ---
*.laccdb # Access lock files

# === Build artifacts & Tools ===

# --- Static Site Generator (SSG) build output ---
# Docusaurus (Selected tool as per devtools-list.md)
build/
.docusaurus/

# --- Other SSG examples (Commented out, for future reference if tool changes) ---
# site/ # MkDocs build output
# .astro/ # Astro build output
# public/ # Common alternative build output directory
# out/ # Common alternative build output directory

# --- RAG Index file (Generated by CI as planned in Task 2.5) ---
index.ndjson

# --- Test coverage reports (For custom scripts) ---
.coverage
coverage/
htmlcov/
nosetests.xml
coverage.xml

# --- Custom Script temporary files (in scripts/ directory) ---
scripts/.cache/
scripts/*.tmp

# === Sensitive data ===
# Environment variables / secrets
.env*
!.env.example # Keep example files if used
*.pem
*.key
credentials.*
config.local.*
secrets.*

# === Miscellaneous ===
# Microsoft Office temporary files
~$*

# --- Project Specific ---
# Add project specific temporary files or build outputs below if necessary
# Example:
# temp_data/
temp-documentation-support/
repomix.config.json
.repomixignore
repomix-output.md
temp-generated-prompt.md